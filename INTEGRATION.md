# 产品整合说明

> 本文件为 flatline 发布包的总装说明，记录产品构成、各部分来源、本次总装所做的整理与校验。

## 一、产品是什么

flatline 是一个给个人投资者用的开源"监测仪表盘工场"，打包为一个可装入任何能执行任务的 AI 助手的技能包。使用者先把自己的投资论点翻译成可观测、可证伪的条件（证伪线 + 预注册信号，收盘口径判定），AI 助手每天跑免费数据管道、对照预注册条件逐条亮灯、按五层结构（谁付钱/钱够不够/钱从哪来/谁能掐断/筹码与杠杆）播报并存档。

它不是买卖信号推送工具，不荐股；它把使用者自己的投资纪律外置成一套可验收的系统。

## 二、包内构成（三层）

| 层 | 目录 | 内容 | 来源 |
|---|---|---|---|
| 方法论层 | `docs/` | 7 篇：纪律军规、执行军规（22条）、建盘指南、存储链双实现实例、验收清单（30条）、免费数据源手册、回测方法示范 | 用户既有积累 |
| 工具层 | `SKILL.md` `scripts/` `templates/` `config.example.json` | 主技能编排（六步流程）、8 个免费数据脚本、五层记分卡空白模板、配置示例 | 用户既有积累 |
| 实例层 | `examples/` `demo/` | A 股存储链真实落地样例（基线/日报/信号表/补充记录/完整开箱包）；虚构演示盘 | A 股实例为 AI 助手从零搭建；demo 为用户既有积累 |
| 根文件 | `README.md` `LICENSE` `.gitignore` | 项目主文档（中英双语）、MIT 许可、Git 忽略规则 | 用户既有积累 |

## 三、各部分来源如实区分

### 用户提供的既有积累（本次总装原样收录，正文一字未改）

- `SKILL.md`、`LICENSE`、`.gitignore`、`config.example.json`
- `docs/` 全部 7 篇方法论文档
- `scripts/` 全部 8 个 Python 数据脚本（共 1365 行）
- `templates/dashboard_template.md`
- `demo/dashboard_demo.md`
- `examples/` 中 4 个原始文件：`baseline_astock_2026-08-14.csv`、`daily_report_no1_2026-08-14.md`、`signal_registry_v1.3.md`、`flatline_A股存储链开箱包_v1.1.2_public.zip`

### 用户提供的既有积累（获批修正一处）
- `README.md`：获批修正一处（目录树中执行军规条数 21→22），其余一字未改

### AI 助手本次总装新建或整理的文件

| 文件 | 性质 | 说明 |
|---|---|---|
| `examples/README.md` | 新建 | 说明 examples 四散件 + zip 的阅读顺序、时序关系、核心池名单差异（安集科技→华海诚科）的来龙去脉、信号口径两次修正的指向 |
| `examples/supplement2_no1_2026-08-16.md` | 从 zip 内提取为散件 | 第 1 期第二次补充记录，原仅存于 A 股开箱包 zip 内，本次总装提至散件层并纳入阅读顺序，内容与 zip 内文件完全一致 |
| `examples/flatline_A股存储链开箱包_v1.1.2_public.zip` | 重新打包（规范格式） | 该内嵌 zip 在流转中曾出现文件名乱码现象；最终包内版本已统一为 UTF-8 标志 + 正斜杠的规范格式，**内容零改动**（哈希对照见第五节） |
| `INTEGRATION.md`（本文件） | 新建 | 总装说明 |

### A 股实现在整体中的位置

`examples/` 里的 A 股存储链盘是 flatline 五层四问哲学在 A 股市场的参考实现，由通用办公 AI 助手在另一个工作流中从零搭建、经多轮人类验收迭代至 v1.1.2（2026-08-16 定版）。它在本产品中的角色是：

1. **可移植性实证**：同一部宪法（预注册/收盘口径/分位纪律/不编数/定版不回改等）在海外盘（美股+韩股，21 条数据序列）和 A 股盘（15 只核心池）上零修改通用，变的只是每层的具体变量和数据通道；
2. **冷启动测试基准**：把开箱包 zip 交给一个全新 AI 助手对话，按包内 SKILL.md 应能独立复现日报；
3. **教学参照**：基线名单与终审名单的差异、信号口径的两次修正，都是"体系是长出来的、规则变更走人类审批"的活教材。

它不改变 flatline 主体的任何文件，只作为 examples 存在。

## 四、本次总装所做的整理

1. **统一目录结构**：所有文件归入 `flatline/` 根目录，保持原始相对路径不变（docs/scripts/templates/demo/examples 层级与原素材一致）。
2. **交叉引用核对**：docs 内 7 篇文档之间的相互引用（discipline↔agent-discipline、build-guide→checklist/data-sources/backtest-method、case-storage→examples 附录）均指向正确文件名；README 目录树与实际文件一致（补充了 examples/README.md）。
3. **排除运行产物**：按 `.gitignore` 规则，`demo_output/`（6 个脚本输出样例 CSV）不进发布包——它是脚本运行输出目录，设计上不进仓库；`config.json`、`flatline-data/`、`__pycache__/` 等被忽略类型同样排除。`examples/` 目录作为明确要发布的样例全部保留（含其中的 CSV）。
4. **规范内嵌 zip 格式**：见下节。
5. **补 examples 阅读指引**：见上节。

## 五、内嵌 A 股开箱包 zip 重打包哈希对照

该内嵌 zip 在流转中曾出现文件名乱码现象。本次总装将其重新打包为 UTF-8 标志 + 正斜杠的规范格式，14 个文件内容逐一 MD5 校验，与定版原件完全一致。乱码的确切成因因原始解压日志未留存，不作断言。

| 文件（包内路径） | MD5（重打包前/后一致） |
|---|---|
| README.md | a558f2e130edbb972e1b981ff30bd5b4 |
| SKILL.md | 21238f5285f6068117dc5ef849407009 |
| 数据区/基线数据表_2026-08-14.csv | 4a587abbc6b16911c9711ab31136fa11 |
| 数据区/基线数据表_2026-08-14.md | b78509fa46345ce687249f14798ca1ba |
| 数据区/存储链日报_第1期_2026-08-14.md | 669712464f71346809c60fdada9e070d |
| 数据区/时间序列_核心池.csv | 780afc3f768c501f6bd9ec79b1c1399f |
| 数据区/第006轮取数记录_日报第1期.md | 6ea676af629c88ccaebbb3f7872f0a05 |
| 数据区/第1期第二次补充记录_2026-08-16.md | 1370c416be2c8922562b750dfda466d3 |
| 数据区/第1期补充记录_2026-08-15.md | 632c936bf19448d1e2fa7ab804d87b7f |
| 规则区/信号预注册表_v1.3定版.md | 9b1aae9e27a261957dcfc3c6f57048b2 |
| 规则区/取数章程_v1.2.md | 069ac7194aecd196fcd76dc3edc397d7 |
| 规则区/存储链股票池_v1.md | 46d2858bd977fab81e754f2026fdc695 |
| 规则区/日报模板_v1.4.md | 944d6ae3cbb12ebc1df1040da8171c23 |
| 规则区/盘框架设计书_v1.md | 67592b65868146681d3bcc533d77132c |

结论：最终包内版本为 UTF-8 标志 + 正斜杠规范格式，14 个文件内容哈希与定版原件逐一一致。

## 六、已知边界

- 所有数据抓取脚本依赖第三方公开接口，可能因对方改版/限流/下线而失效；脚本设计为"拉不到就报错留空，不编数"。
- A 股实例中的数据通道（iFinD 等）依赖使用者自己的 AI 助手环境配置，开箱包内只含数据与规则，不含连接器凭证。
- 实例带时间戳（截至 2026-08），内容会过时；方法不会。
- 本产品为研究工具，不构成投资建议。
