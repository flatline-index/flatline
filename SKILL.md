---
name: flatline
description: 刷新投研监测仪表盘并播报信号。当用户输入 /flatline,或说"刷一下仪表盘/更新仪表盘/跑一下日更/看看今天的盘面数据"时使用。跑 config.json 里启用的数据脚本组攒 csv、拉辅助数据、按现行快照里预注册的信号逐条判定、落当日新快照到 config 配置的目录,并在对话里按层结构播报。只刷数据和判定,预注册条件(证伪线/信号定义/触发-动作)一个字不改。
---

# flatline · 仪表盘日更(数据 + 信号播报)

被呼出即全套执行,不问问题;拉不到的数据标"待填",绝不编数;历史分位样本不足时写
"历史不足N天,分位暂缺"。

运行环境:本技能假设 `config.json`(没有就退 `config.example.json` 的内置默认值)和
`scripts/` 放在本仓库根目录——把整个 flatline/ 目录复制进你的 AI 助手技能目录即可,
或按你自己的目录习惯调整下面命令里的路径。

## 第 0 步:校时

取本机当前时间,换算出你监测标的所在市场的收盘时间与数据定版时间点。本仓库随附脚本
默认覆盖韩股与美股/全球数据源,示例换算(太平洋时区起算):首尔=太平洋+16h,美东=
太平洋+3h(均为夏令时口径,冬令时各差 1 小时)。用途:
- 本地市场收盘前抓到的当日数据是**临时值**,引用必须标"临时"
- 当日机构/外资净买、融资余额类数据通常要当地市场收盘后数小时才定版,白天跑到的最新
  一行往往还是前一交易日
- 跨市场配对(如 ADR 溢价脚本)时,尚未收盘的一侧配的是另一侧"上一个已完成交易日"

换成其他市场的标的,自己算好对应时区差与定版延迟,道理不变。

## 第 1 步:读现行状态

- 读 `<dashboards_dir>` 下文件名日期**最新**的 `dashboard_<主题>_*.md`——证伪线、预注册
  信号、触发-动作对照、各指标阈值都登记在里面,本次判定以它为准
- 备查(**不必每次读**,通道出问题或口径存疑时再翻):`docs/data-sources.md`(通道与
  口径手册)、`docs/discipline.md`(纪律军规)
- 一个 `dashboards_dir` 下可以同时存在多个主题的快照,见文末"多盘扩展"

## 第 2 步:跑启用的脚本(注意解释器)

读 `config.json` 的 `scripts` 段,只跑 `enabled: true` 的项;每项按 `interpreter` 字段
选用 `python_cmd` 还是 `python_cmd_scrapling`(两个字段各自对应的实际命令也在
`config.json` 里,例如某些环境下 scrapling 只装在特定 Python 版本,`python_cmd_scrapling`
就该填那个命令)。八个脚本默认全部启用:

```
<python_cmd>            scripts/adr_premium.py
<python_cmd>            scripts/kofia_margin.py
<python_cmd_scrapling>  scripts/openrouter_tokens.py
<python_cmd>            scripts/gpu_rent.py
<python_cmd>            scripts/kr_rates.py
<python_cmd_scrapling>  scripts/cds_ice.py
```

- 记下各自输出的当前值、历史分位、环比,csv 自动落进 `config.json` 里配的 `dashboards_dir`
- `adr_premium` 全程走免费公开源(Naver + 汇率 API),无需任何本地客户端或账号
- 任一脚本挂掉:如实记录错误,其余照跑,不因一条腿断了停整个流程

## 第 3 步:辅助数据与定期锚

日常再跑:
```
<python_cmd> scripts/krx_flow.py         # 不给参数则批量跑 config.json 的 krx_flow_codes
<python_cmd> scripts/news_watch.py       # 只报新增,层④事件锚
```

- 层④ 事件锚:`news_watch.py` 只播报新增条目。有新增时按信源分级判读:一级=公司财报会
  /公告原话,二级=行业研究机构报告,三级=媒体报道(播报一律带"传闻,未证实")。是否
  触发层④黄/红灯,按现行快照登记的状态标准判;红灯必须一级或二级信源确认,并触发快照
  里对应的触发编号
- 月锚(如果你的监测计划里有低频更新的定性数据源,比如行业价格通讯稿):每月固定一次
  去核一次,更新对应层
- 季锚(如果层②/③配置了财报相关指标,如现金流、资本开支):每个财报季后重新拉取最新
  一期数据,手动或让 AI 助手帮你追加进对应 csv
- 你可以按需再加其他手动检查项(比如某个波动率指数、某个衍生品对理论价的偏离度)——
  这类偏门数据源通常没有通用脚本,自己按需现学现抓,抓法可以参考 `docs/data-sources.md`
  里已验证通道的写法(编码、分页、口径处理这些坑是通用的)

## 第 4 步:信号判定

按现行快照登记的预注册信号逐条对照新数据,给状态:成立 / 成立候选 / 未触发 / 待验证。
通用口径纪律(口径三问,详见 `docs/discipline.md`):这个数字的统计边界是什么、样本有
没有偏移、现在看到的是水平还是趋势。

本仓库自带脚本已知的口径坑(用其他数据源要自己排查类似陷阱):
- `kofia_margin` 的强平数据对"前一日未收金"执行,比大跌滞后 1-2 个交易日——大跌次日
  别急着说"没有强平潮"
- `krx_flow` 的外资/机构净买以**盘后定版数**为准,盘中估计行不算
- 任何"当日反转/次日不破低"类判断,用定版收盘与日内低点判,临时值窗口内只能标"待验证"

## 第 5 步:落新快照

- 复制现行快照为 `<dashboards_dir>/dashboard_<主题>_<今天YYYY-MM-DD>.md`;同日重复呼出
  则直接更新当日这份
- 更新:头部生成时点与新鲜度注、各层指标表当前值/历史分位/状态灯、信号板读数与判定、
  复盘记录加一行
- 旧快照不删不改
- **不许动**:证伪线、信号定义、触发-动作对照的条件、事件日历里未登记的新事件(不预测
  财报/事件日期)。这些是你自己预注册的,只有你自己改——AI 只负责刷数据和判定,不许
  "顺手"帮你优化预注册的规则

## 第 6 步:对话播报(直接把这段读给用户)

- 第一句给结论:信号板 X/N(N=现行快照里预注册的信号总数)、当前体制维持还是有变化。
  若有信号从"未触发"转为"成立候选/成立",第一行用【信号】开头置顶写明哪条、依据什么数
- 然后**严格按仪表盘的层结构分块播报**(不要打平成一条条流水账),每层块首标状态灯,
  没启用的脚本/没配置的层直接跳过,待填的项也要报"待填 + 等什么",不许悄悄省略:
  - **层① 谁付钱(需求)**:默认对应 `openrouter_tokens`(**必带口径:仅经该平台路由的
    流量,不代表全球总量,当趋势不当总量**)、`gpu_rent`(按你配置的卡型分组报时租)
  - **层② 钱够不够(现金流/盈利能见度)**:默认没有配套脚本——这层的数据源通常低频、
    需要人工或订阅渠道获取(行业报告、财报),按你自己登记的数据源报读数,注明"数据
    截至 X 期",没更新也要出现,不许省略
  - **层③ 钱从哪来(资金/融资)**:默认对应 `cds_ice`(近似利差、公司名 + 异动)
  - **层④ 谁能掐断(约束/政策)**:默认对应 `news_watch`(新增 N 条及分级,无新增一句
    带过)
  - **层⑤ 筹码与杠杆**:默认对应 `kofia_margin`(融资余额/强平)、`krx_flow`(机构/外资
    净买)、`adr_premium`(ADR 溢价)、`kr_rates`(流动性代理利差,注明自建口径)
- 最后:下一检查点看什么 + 落盘位置(`<dashboards_dir>`) + 哪些待填 + 哪条腿出错(如有)
- 平实中文,不堆格式;数字给出处日期,临时值标"临时"

## 多盘扩展

一个 `dashboards_dir` 下可以维护多个主题的快照(比如同时监测"存储"和"医疗"两个盘)。
用户点名哪个刷哪个;没点名,默认把 `dashboards_dir` 下所有"现行版"快照各刷一遍(本仓库
自带的 8 个脚本是通用数据管道,不特定于某个主题;不同主题各自的"层②供给侧数据"这类
主题专属数据源,按各自快照里登记的数据源单独处理,不在这 8 个脚本覆盖范围内)。
